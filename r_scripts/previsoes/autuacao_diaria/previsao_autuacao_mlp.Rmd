---
title: "previsao_autuacao_mlp"
output: html_document
---

## Dependências

```{r}
library(daltoolbox)
library(readr)
library(lubridate)
library(dplyr)
library(ggplot2)
library(zoo) 


```

## Processamento

### Infrações diárias

-   **df_autuacao:** dataframe com as observações diárias das infrações de trânsito

    (data, status_infracao, valor_pago, especie_veiculo, data_pagamento, data_limite_recurso, descricao_autuacao, tipo_veiculo)

```{r}
df <- data.frame(df_autuacao)

# Converte o tipo das datas
df <- df %>%
  mutate(
    data = as.Date(data) 
  )
```

```{r}
head(df)
```

## Agrupamento por dia

Filtra as ocorrências com valor útil\
Agrupa pelo dia de pagamento\
(o filtro da data é por conta de ruídos)

-   **valor_pago_total:** soma das multas pagas do

-   **media_tempo_pagamento:** média do tempo de pagamento em dias\

```{r}
df_ordenado_valor_pago_total <- df %>%
  filter(
    !is.na(valor_pago) &
    valor_pago > 0.0 &
    data_pagamento > "2014-01-01" &
    data_pagamento < "2025-01-01") %>%
  mutate(
    tempo_pagamento = as.numeric(data_pagamento - data, units = "days")
  ) %>%
  group_by(data_pagamento) %>%
  summarise(
    valor_pago_total = sum(valor_pago, na.rm = TRUE),
    media_tempo_pagamento = mean(tempo_pagamento, na.rm = TRUE)
  ) %>%
  select(data_pagamento, valor_pago_total, media_tempo_pagamento)  

df_ordenado_valor_pago_total$data_pagamento <- as.Date(df_ordenado_valor_pago_total$data_pagamento)
```

### Previsao

transformar em st

```{r}
ts_valor_pago_total_dia <- ts_data(df_ordenado_valor_pago_total$valor_pago_total, 7)


```

separação teste/treino

```{r}

samp <- ts_sample(ts_valor_pago_total_dia, test_size = 30)

io_train <- ts_projection(samp$train)
io_test <- ts_projection(samp$test)

```

modelo e ajuste

```{r}
preproc <- ts_norm_ean()

#treinamento modelo
model <- ts_mlp(preproc, input_size=2, size=4, decay=0)
model <- fit(model, x=io_train$input, y=io_train$output)

#ajuste
adjust <- predict(model, io_train$input)
adjust <- as.vector(adjust)
output <- as.vector(io_train$output)
ev_adjust <- evaluate(model, output, adjust)
ev_adjust$mse
```

predição

```{r}
#prediçao
prediction <- predict(model, x=io_test$input, steps_ahead=1)
prediction <- as.vector(prediction)
output <- as.vector(io_test$output)
ev_test <- evaluate(model, output, prediction)
ev_test


```

plot

```{r}
yvalues <- c(io_train$output, io_test$output)

plot_ts_pred(y=yvalues, yadj=adjust, ypre=prediction) + theme(text = element_text(size=14))
```

## Agrupamento por mês

```{r}
# Criar dataframe com valor_pago_mensal
df_mensal <- df_ordenado_valor_pago_total %>%
  mutate(
    ano_mes = floor_date(data_pagamento, "month")  # Extrai o primeiro dia do mês,
  ) %>%
  group_by(ano_mes) %>%  # Agrupa por mês
  summarise(
    valor_pago_mensal = sum(valor_pago_total, na.rm = TRUE),  # Soma os valores do mês
    media_tempo_pagamento = mean(tempo_pagamento, na.rm = TRUE)
  ) %>%
  rename(data_pagamento = ano_mes)  # Renomeia para manter o padrão

# Resultado
head(df_mensal)
```

### plot

```{r}
df_mensal$data_pagamento <- as.Date(df_mensal$data_pagamento)

# Plot com group = 1 e pontos
ggplot(df_mensal, aes(x = data_pagamento, y = valor_pago_mensal)) +
  geom_line(group = 1, color = "steelblue", linewidth = 0.5) +  # Força a linha
  labs(
    title = "Valor do Pagamento por MEs",
    x = "Dia do Pagamento",
    y = "Valor Total Pago (R$)"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

```
