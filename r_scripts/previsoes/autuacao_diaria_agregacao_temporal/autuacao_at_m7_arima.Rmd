---
title: "autuacao_semanal_arima"
output: html_document
---

## Dependências

```{r}
library(daltoolbox)
library(daltoolboxdp)
library(readr)
library(lubridate)
library(dplyr)
library(ggplot2)
library(zoo) 
source("../validacao/validacao_cruzada_plot.R")

```

-   **df_autuacao:** dataframe com as observações diárias das infrações de trânsito

    (data, status_infracao, valor_pago, especie_veiculo, data_pagamento, data_limite_recurso, descricao_autuacao, tipo_veiculo)

```{r}
if (!exists("df") || is.null(df) || is.function(df)) {
  df <- data.frame(df_autuacao)
}
```

```{r}
head(df)
```

## Pré-processamento

```{r}
df_ordenado_valor_pago_total_semana <- df %>%
  filter(
    !is.na(valor_pago) &
    valor_pago > 0.0 &
    data_pagamento > "2016-01-01" &
    data_pagamento < "2025-01-01"  ) %>%
  mutate(
    data = as.Date(data)
  ) 

```

## Processamento

data_pagamento

```{r}
df_ordenado_valor_pago_total_semana <- df_ordenado_valor_pago_total_semana %>%
  mutate(
    semana_pagamento = floor_date(data_pagamento, "week")#ajustar pra nao ficar a ultima semana com dias faltantes
  ) %>%
  group_by(semana_pagamento) %>%
  summarise(
    valor_pago_total = sum(valor_pago, na.rm = TRUE)
  ) %>%
  select(semana_pagamento, valor_pago_total) 

df_ordenado_valor_pago_total_semana$semana_pagamento <- as.Date(df_ordenado_valor_pago_total_semana$semana_pagamento)


```

transformar em st

```{r}
ts_valor_pago_total_semana <- ts_data(df_ordenado_valor_pago_total_semana$valor_pago_total, 0)
ts_data <- df_ordenado_valor_pago_total_semana$semana_pagamento
```

## Separação teste/treino

```{r}

samp <- ts_sample(ts_valor_pago_total_semana, test_size = 36)

io_train <- ts_projection(samp$train)
io_test <- ts_projection(samp$test)

```

## Modelo e ajuste dos parâmetros

```{r}
#treinamento modelo
model <- ts_arima()
model <- fit(model, x=io_train$input, y=io_train$output)

#ajuste
adjust <- predict(model, io_train$input)
adjust <- as.vector(adjust)
output <- as.vector(io_train$output)
ev_adjust <- evaluate(model, output, adjust)
#ev_adjust$mse
```

## Predição e resultado

```{r}
prediction <- predict(model, x=io_test$input, steps_ahead=length(io_test$input))
prediction <- as.vector(prediction)
output <- as.vector(io_test$output)
ev_test <- evaluate(model, output, prediction)
ev_test


```

## Plot do gráfico

```{r}
yvalues <- c(io_train$output, io_test$output)

plot_ts_pred(y=yvalues, yadj=adjust, ypre=prediction) + theme(text = element_text(size=14))

plot_data <- data.frame(
  data = ts_data,  
  yvalues = yvalues,
  adjust = c(adjust, rep(NA, length(yvalues) - length(adjust))),
  prediction = c(rep(NA, length(yvalues) - length(prediction)), prediction)
)

data_inicio <- as.Date("2016-01-01")
data_fim <- as.Date("2025-01-01")

ggplot(plot_data, aes(x = data)) +
  geom_point(aes(y = yvalues), color = "#999") +
  geom_line(aes(y = yvalues), color = "#555") + 
  
  geom_line(aes(y = adjust), color = "blue", linetype = "dashed") +
  geom_line(aes(y = prediction), color = "green", linetype = "dashed") +
  scale_linetype_manual(
    name = "Legenda",
    values = c("solid", "dashed", "dashed"),
    labels = c("Valores Reais", "Valores Ajustados", "Previsões")
  ) +
  labs(title = "Série Temporal de Receitas",
       x = "Período", y = "Valor Total Pago") +
  theme_minimal() +
  coord_cartesian(xlim = c(data_inicio, data_fim)) +
  guides(color = "none")
```

## Tamanhos

```{r}
cat("Tamanhos:\nSérie:", length(yvalues), "\n",
    "Teste:", length(io_test$input), "\n",
    "Steps Ahead:", length(prediction), "\n",
    "Porcentagem Teste:", sprintf("%.2f%%", length(prediction)/length(yvalues)*100), "\n")
```

```{r}
QTD_TESTES <- 3
TAMANHO_TESTE <- 24
TAMANHO_TREINO <- length(yvalues) - (QTD_TESTES * TAMANHO_TESTE) 
PASSO <- TAMANHO_TESTE 

resultados_cv <- validacao_cruzada_plot_ts(
  serie_temporal = df_ordenado_valor_pago_total_semana$valor_pago_total,
  datas_ts = df_ordenado_valor_pago_total_semana$semana_pagamento,
  tam_janela_treino = TAMANHO_TREINO,
  tam_janela_teste = TAMANHO_TESTE,
  step = PASSO,
  modelo_func = function() { ts_arima() },
  zoom_dias_antes_previsao = 180
)

# --- Análise dos Resultados ---

# 1. Imprimir as métricas médias
cat("\n--- MÉTRICAS MÉDIAS DA VALIDAÇÃO CRUZADA ---\n")
print(resultados_cv$media_metricas)
plotar_todos_os_folds(resultados_cv$plots_folds)
```
